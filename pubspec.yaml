name: imitatio
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  crypto: ^3.0.3

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.1
